name: Sync with Template Repository

author: liblaf

description: Update current repository with template repository

runs:
  using: composite
  steps:
    - run: make --always-make --directory="${{ github.workspace }}" --makefile="${{ github.action_path }}/init.mk"
      shell: bash

branding:
  color: blue
  icon: refresh-cw
